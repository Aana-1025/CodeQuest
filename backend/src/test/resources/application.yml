spring:
  datasource:
    url: jdbc:h2:mem:codequest;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  flyway:
    enabled: false
jwt:
  secret: test-jwt-secret-dev-only-change-this-secret
  access-minutes: 15
auth:
  refresh-token-days: 7
